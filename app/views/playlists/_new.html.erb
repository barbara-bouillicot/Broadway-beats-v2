<div class="container my-3">

  <%= simple_form_for @playlist do |f| %>
    <%= f.input :name %>
    <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit class:"btn-app", value: "Create playlist"%>
<% end %>
</div>
